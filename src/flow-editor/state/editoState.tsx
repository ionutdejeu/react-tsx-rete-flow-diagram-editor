import { IEditorItem } from "../../shared/types"
import { DynamicNode } from "../nodes/dynamicNode"


export interface IReteEditorStateNode {
    n:DynamicNode,
    i:IEditorItem
}
export interface IReteEditorStateNode {
    n:DynamicNode,
    i:IEditorItem
}

export interface IReteEditorState {
    
}